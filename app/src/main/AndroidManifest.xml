<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.pitongku.fishinfo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activity.GraphActivity"/>
        <activity
            android:name=".Activity.ListHargaActivity"
            android:label="@string/title_activity_list_harga"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.MenuHargaActivity" />
        <activity
            android:name=".Activity.InputHargaKonsumsiActivity"
            android:label="@string/title_activity_input_harga_konsumsi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.KatalogActivity" />
        <activity android:name=".Activity.IkanKonsumsiActivity" />
        <activity android:name=".Activity.InputJmlActivity" />
        <activity
            android:name=".Activity.InputActivity"
            android:theme="@style/NoActionBar" />
        <activity android:name=".Activity.HomeActivity" />
        <activity android:name=".Activity.LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>